"""Base class for the Tk common dialogs."""

from collections.abc import Mapping
from tkinter import Misc
from typing import ClassVar

__all__ = ["Dialog"]

class Dialog:
    command: ClassVar[str?]
    master: Misc?
    # Types of options are very dynamic. They depend on the command and are
    # sometimes changed to a different type.
    options: Mapping[str, dynamic]
    init(self, master: Misc? = None, **options: dynamic)
    def show(self, **options: dynamic) -> dynamic

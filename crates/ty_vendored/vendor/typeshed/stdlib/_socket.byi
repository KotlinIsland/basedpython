"""Implementation module for socket operations.

See the socket module for documentation.
"""

import sys
from _typeshed import ReadableBuffer, WriteableBuffer
from collections.abc import Iterable
from socket export error, gaierror, herror, timeout
from typing import Final, TypeAlias
from typing_extensions import CapsuleType, disjoint_base

private type CMSG = (int, int, bytes)
private type CMSGArg = (int, int, ReadableBuffer)

# Addresses can be either tuples of varying lengths (AF_INET, AF_INET6,
# AF_NETLINK, AF_TIPC) or strings/buffers (AF_UNIX).
# See getsockaddrarg() in socketmodule.c.
if sys.version_info >= (3, 14):
    # A bare int is accepted for Bluetooth HCI device IDs.
    type _Address = (*: dynamic) | str | ReadableBuffer | int
else:
    type _Address = (*: dynamic) | str | ReadableBuffer
type _RetAddress = dynamic

# ===== Constants =====
# This matches the order in the CPython documentation
# https://docs.python.org/3/library/socket.html#constants

if sys.platform != "win32":
    final AF_UNIX: int

final AF_INET: int
final AF_INET6: int

final AF_UNSPEC: int

final SOCK_STREAM: int
final SOCK_DGRAM: int
final SOCK_RAW: int
final SOCK_RDM: int
final SOCK_SEQPACKET: int

if sys.platform == "linux":
    # Availability: Linux >= 2.6.27
    final SOCK_CLOEXEC: int
    final SOCK_NONBLOCK: int

# --------------------
# Many constants of these forms, documented in the Unix documentation on
# sockets and/or the IP protocol, are also defined in the socket module.
# SO_*
# socket.SOMAXCONN
# MSG_*
# SOL_*
# SCM_*
# IPPROTO_*
# IPPORT_*
# INADDR_*
# IP_*
# IPV6_*
# EAI_*
# AI_*
# NI_*
# TCP_*
# --------------------

final SO_ACCEPTCONN: int
final SO_BROADCAST: int
final SO_DEBUG: int
final SO_DONTROUTE: int
final SO_ERROR: int
final SO_KEEPALIVE: int
final SO_LINGER: int
final SO_OOBINLINE: int
final SO_RCVBUF: int
final SO_RCVLOWAT: int
final SO_RCVTIMEO: int
final SO_REUSEADDR: int
final SO_SNDBUF: int
final SO_SNDLOWAT: int
final SO_SNDTIMEO: int
final SO_TYPE: int
if sys.platform != "linux":
    final SO_USELOOPBACK: int
if sys.platform == "win32":
    final SO_EXCLUSIVEADDRUSE: int
if sys.platform != "win32":
    final SO_REUSEPORT: int
    if sys.platform != "darwin" or sys.version_info >= (3, 13):
        final SO_BINDTODEVICE: int

if sys.platform != "win32" and sys.platform != "darwin":
    final SO_DOMAIN: int
    final SO_MARK: int
    final SO_PASSCRED: int
    final SO_PASSSEC: int
    final SO_PEERCRED: int
    final SO_PEERSEC: int
    final SO_PRIORITY: int
    final SO_PROTOCOL: int
if sys.platform != "win32" and sys.platform != "darwin" and sys.platform != "linux":
    final SO_SETFIB: int
if sys.platform == "linux" and sys.version_info >= (3, 13):
    final SO_BINDTOIFINDEX: int

final SOMAXCONN: int

final MSG_CTRUNC: int
final MSG_DONTROUTE: int
final MSG_OOB: int
final MSG_PEEK: int
final MSG_TRUNC: int
final MSG_WAITALL: int
if sys.platform != "win32":
    final MSG_DONTWAIT: int
    final MSG_EOR: int
    final MSG_NOSIGNAL: int  # Sometimes this exists on darwin, sometimes not
if sys.platform != "darwin":
    final MSG_ERRQUEUE: int
if sys.platform == "win32":
    final MSG_BCAST: int
    final MSG_MCAST: int
if sys.platform != "win32" and sys.platform != "darwin":
    final MSG_CMSG_CLOEXEC: int
    final MSG_CONFIRM: int
    final MSG_FASTOPEN: int
    final MSG_MORE: int
if sys.platform != "win32" and sys.platform != "linux":
    final MSG_EOF: int
if sys.platform != "win32" and sys.platform != "linux" and sys.platform != "darwin":
    final MSG_NOTIFICATION: int
    final MSG_BTAG: int  # Not FreeBSD either
    final MSG_ETAG: int  # Not FreeBSD either

final SOL_IP: int
final SOL_SOCKET: int
final SOL_TCP: int
final SOL_UDP: int
if sys.platform != "win32" and sys.platform != "darwin":
    # Defined in socket.h for Linux, but these aren't always present for
    # some reason.
    final SOL_ATALK: int
    final SOL_AX25: int
    final SOL_HCI: int
    final SOL_IPX: int
    final SOL_NETROM: int
    final SOL_ROSE: int

if sys.platform != "win32":
    final SCM_RIGHTS: int
if sys.platform != "win32" and sys.platform != "darwin":
    final SCM_CREDENTIALS: int
if sys.platform != "win32" and sys.platform != "linux":
    final SCM_CREDS: int

final IPPROTO_ICMP: int
final IPPROTO_IP: int
final IPPROTO_RAW: int
final IPPROTO_TCP: int
final IPPROTO_UDP: int
final IPPROTO_AH: int
final IPPROTO_DSTOPTS: int
final IPPROTO_EGP: int
final IPPROTO_ESP: int
final IPPROTO_FRAGMENT: int
final IPPROTO_HOPOPTS: int
final IPPROTO_ICMPV6: int
final IPPROTO_IDP: int
final IPPROTO_IGMP: int
final IPPROTO_IPV6: int
final IPPROTO_NONE: int
final IPPROTO_PIM: int
final IPPROTO_PUP: int
final IPPROTO_ROUTING: int
final IPPROTO_SCTP: int
if sys.platform != "linux":
    final IPPROTO_GGP: int
    final IPPROTO_IPV4: int
    final IPPROTO_MAX: int
    final IPPROTO_ND: int
if sys.platform == "win32":
    final IPPROTO_CBT: int
    final IPPROTO_ICLFXBM: int
    final IPPROTO_IGP: int
    final IPPROTO_L2TP: int
    final IPPROTO_PGM: int
    final IPPROTO_RDP: int
    final IPPROTO_ST: int
if sys.platform != "win32":
    final IPPROTO_GRE: int
    final IPPROTO_IPIP: int
    final IPPROTO_RSVP: int
    final IPPROTO_TP: int
if sys.platform != "win32" and sys.platform != "linux":
    final IPPROTO_EON: int
    final IPPROTO_HELLO: int
    final IPPROTO_IPCOMP: int
    final IPPROTO_XTP: int
if sys.platform != "win32" and sys.platform != "darwin" and sys.platform != "linux":
    final IPPROTO_BIP: int  # Not FreeBSD either
    final IPPROTO_MOBILE: int  # Not FreeBSD either
    final IPPROTO_VRRP: int  # Not FreeBSD either
if sys.platform == "linux":
    # Availability: Linux >= 2.6.20, FreeBSD >= 10.1
    final IPPROTO_UDPLITE: int
if sys.platform == "linux":
    final IPPROTO_MPTCP: int

final IPPORT_RESERVED: int
final IPPORT_USERRESERVED: int

final INADDR_ALLHOSTS_GROUP: int
final INADDR_ANY: int
final INADDR_BROADCAST: int
final INADDR_LOOPBACK: int
final INADDR_MAX_LOCAL_GROUP: int
final INADDR_NONE: int
final INADDR_UNSPEC_GROUP: int

final IP_ADD_MEMBERSHIP: int
final IP_DROP_MEMBERSHIP: int
final IP_HDRINCL: int
final IP_MULTICAST_IF: int
final IP_MULTICAST_LOOP: int
final IP_MULTICAST_TTL: int
final IP_OPTIONS: int
if sys.platform != "linux":
    final IP_RECVDSTADDR: int
final IP_RECVTOS: int
final IP_TOS: int
final IP_TTL: int
if sys.platform != "win32":
    final IP_DEFAULT_MULTICAST_LOOP: int
    final IP_DEFAULT_MULTICAST_TTL: int
    final IP_MAX_MEMBERSHIPS: int
    final IP_RECVOPTS: int
    final IP_RECVRETOPTS: int
    final IP_RETOPTS: int
if sys.version_info >= (3, 13) and sys.platform == "linux":
    final CAN_RAW_ERR_FILTER: int
if sys.version_info >= (3, 15):
    if sys.platform == "win32" or sys.platform == "linux":
        final IPV6_HDRINCL: int
if sys.version_info >= (3, 14):
    final IP_RECVTTL: int

    if sys.platform == "win32" or sys.platform == "linux":
        final IPV6_RECVERR: int
        final IP_RECVERR: int
        final SO_ORIGINAL_DST: int

    if sys.platform == "win32":
        final SOL_RFCOMM: int
        final SO_BTH_ENCRYPT: int
        final SO_BTH_MTU: int
        final SO_BTH_MTU_MAX: int
        final SO_BTH_MTU_MIN: int
        final TCP_QUICKACK: int

    if sys.platform == "linux":
        final BDADDR_BREDR: int
        final BDADDR_LE_PUBLIC: int
        final BDADDR_LE_RANDOM: int
        final BT_CHANNEL_POLICY: int
        final BT_CHANNEL_POLICY_BREDR_ONLY: int
        final BT_CHANNEL_POLICY_BREDR_PREFERRED: int
        final BT_CODEC: int
        final BT_DEFER_SETUP: int
        final BT_FLUSHABLE: int
        final BT_FLUSHABLE_OFF: int
        final BT_FLUSHABLE_ON: int
        final BT_ISO_QOS: int
        final BT_MODE: int
        final BT_MODE_BASIC: int
        final BT_MODE_ERTM: int
        final BT_MODE_EXT_FLOWCTL: int
        final BT_MODE_LE_FLOWCTL: int
        final BT_MODE_STREAMING: int
        final BT_PHY: int
        final BT_PHY_BR_1M_1SLOT: int
        final BT_PHY_BR_1M_3SLOT: int
        final BT_PHY_BR_1M_5SLOT: int
        final BT_PHY_EDR_2M_1SLOT: int
        final BT_PHY_EDR_2M_3SLOT: int
        final BT_PHY_EDR_2M_5SLOT: int
        final BT_PHY_EDR_3M_1SLOT: int
        final BT_PHY_EDR_3M_3SLOT: int
        final BT_PHY_EDR_3M_5SLOT: int
        final BT_PHY_LE_1M_RX: int
        final BT_PHY_LE_1M_TX: int
        final BT_PHY_LE_2M_RX: int
        final BT_PHY_LE_2M_TX: int
        final BT_PHY_LE_CODED_RX: int
        final BT_PHY_LE_CODED_TX: int
        final BT_PKT_STATUS: int
        final BT_POWER: int
        final BT_POWER_FORCE_ACTIVE_OFF: int
        final BT_POWER_FORCE_ACTIVE_ON: int
        final BT_RCVMTU: int
        final BT_SECURITY: int
        final BT_SECURITY_FIPS: int
        final BT_SECURITY_HIGH: int
        final BT_SECURITY_LOW: int
        final BT_SECURITY_MEDIUM: int
        final BT_SECURITY_SDP: int
        final BT_SNDMTU: int
        final BT_VOICE: int
        final BT_VOICE_CVSD_16BIT: int
        final BT_VOICE_TRANSPARENT: int
        final BT_VOICE_TRANSPARENT_16BIT: int
        final HCI_CHANNEL_CONTROL: int
        final HCI_CHANNEL_LOGGING: int
        final HCI_CHANNEL_MONITOR: int
        final HCI_CHANNEL_RAW: int
        final HCI_CHANNEL_USER: int
        final HCI_DEV_NONE: int
        final IP_FREEBIND: int
        final IP_RECVORIGDSTADDR: int
        final L2CAP_LM: int
        final L2CAP_LM_AUTH: int
        final L2CAP_LM_ENCRYPT: int
        final L2CAP_LM_MASTER: int
        final L2CAP_LM_RELIABLE: int
        final L2CAP_LM_SECURE: int
        final L2CAP_LM_TRUSTED: int
        final SOL_BLUETOOTH: int
        final SOL_L2CAP: int
        final SOL_RFCOMM: int
        final SOL_SCO: int
        final VMADDR_CID_LOCAL: int

if sys.platform != "win32" and sys.platform != "darwin":
    final IP_TRANSPARENT: int
if sys.platform != "win32" and sys.platform != "darwin" and sys.version_info >= (3, 11):
    final IP_BIND_ADDRESS_NO_PORT: int
if sys.version_info >= (3, 12):
    final IP_ADD_SOURCE_MEMBERSHIP: int
    final IP_BLOCK_SOURCE: int
    final IP_DROP_SOURCE_MEMBERSHIP: int
    final IP_PKTINFO: int
    final IP_UNBLOCK_SOURCE: int

final IPV6_CHECKSUM: int
final IPV6_JOIN_GROUP: int
final IPV6_LEAVE_GROUP: int
final IPV6_MULTICAST_HOPS: int
final IPV6_MULTICAST_IF: int
final IPV6_MULTICAST_LOOP: int
final IPV6_RECVTCLASS: int
final IPV6_TCLASS: int
final IPV6_UNICAST_HOPS: int
final IPV6_V6ONLY: int
final IPV6_DONTFRAG: int
final IPV6_HOPLIMIT: int
final IPV6_HOPOPTS: int
final IPV6_PKTINFO: int
final IPV6_RECVRTHDR: int
final IPV6_RTHDR: int
if sys.platform != "win32":
    final IPV6_RTHDR_TYPE_0: int
    final IPV6_DSTOPTS: int
    final IPV6_NEXTHOP: int
    final IPV6_PATHMTU: int
    final IPV6_RECVDSTOPTS: int
    final IPV6_RECVHOPLIMIT: int
    final IPV6_RECVHOPOPTS: int
    final IPV6_RECVPATHMTU: int
    final IPV6_RECVPKTINFO: int
    final IPV6_RTHDRDSTOPTS: int

if sys.platform != "win32" and sys.platform != "linux":
    final IPV6_USE_MIN_MTU: int

final EAI_AGAIN: int
final EAI_BADFLAGS: int
final EAI_FAIL: int
final EAI_FAMILY: int
final EAI_MEMORY: int
final EAI_NODATA: int
final EAI_NONAME: int
final EAI_SERVICE: int
final EAI_SOCKTYPE: int
if sys.platform != "win32":
    final EAI_ADDRFAMILY: int
    final EAI_OVERFLOW: int
    final EAI_SYSTEM: int
if sys.platform != "win32" and sys.platform != "linux":
    final EAI_BADHINTS: int
    final EAI_MAX: int
    final EAI_PROTOCOL: int

final AI_ADDRCONFIG: int
final AI_ALL: int
final AI_CANONNAME: int
final AI_NUMERICHOST: int
final AI_NUMERICSERV: int
final AI_PASSIVE: int
final AI_V4MAPPED: int
if sys.platform != "win32" and sys.platform != "linux":
    final AI_DEFAULT: int
    final AI_MASK: int
    final AI_V4MAPPED_CFG: int

final NI_DGRAM: int
final NI_MAXHOST: int
final NI_MAXSERV: int
final NI_NAMEREQD: int
final NI_NOFQDN: int
final NI_NUMERICHOST: int
final NI_NUMERICSERV: int
if sys.platform == "linux" and sys.version_info >= (3, 13):
    final NI_IDN: int

final TCP_FASTOPEN: int
final TCP_KEEPCNT: int
final TCP_KEEPINTVL: int
final TCP_MAXSEG: int
final TCP_NODELAY: int
if sys.platform != "win32":
    final TCP_NOTSENT_LOWAT: int
if sys.platform != "darwin":
    final TCP_KEEPIDLE: int
if sys.platform == "darwin":
    final TCP_KEEPALIVE: int
if sys.version_info >= (3, 11) and sys.platform == "darwin":
    final TCP_CONNECTION_INFO: int

if sys.platform != "win32" and sys.platform != "darwin":
    final TCP_CONGESTION: int
    final TCP_CORK: int
    final TCP_DEFER_ACCEPT: int
    final TCP_INFO: int
    final TCP_LINGER2: int
    final TCP_QUICKACK: int
    final TCP_SYNCNT: int
    final TCP_USER_TIMEOUT: int
    final TCP_WINDOW_CLAMP: int
if sys.platform == "linux" and sys.version_info >= (3, 12):
    final TCP_CC_INFO: int
    final TCP_FASTOPEN_CONNECT: int
    final TCP_FASTOPEN_KEY: int
    final TCP_FASTOPEN_NO_COOKIE: int
    final TCP_INQ: int
    final TCP_MD5SIG: int
    final TCP_MD5SIG_EXT: int
    final TCP_QUEUE_SEQ: int
    final TCP_REPAIR: int
    final TCP_REPAIR_OPTIONS: int
    final TCP_REPAIR_QUEUE: int
    final TCP_REPAIR_WINDOW: int
    final TCP_SAVED_SYN: int
    final TCP_SAVE_SYN: int
    final TCP_THIN_DUPACK: int
    final TCP_THIN_LINEAR_TIMEOUTS: int
    final TCP_TIMESTAMP: int
    final TCP_TX_DELAY: int
    final TCP_ULP: int
    final TCP_ZEROCOPY_RECEIVE: int

# --------------------
# Specifically documented constants
# --------------------

if sys.platform == "linux":
    # Availability: Linux >= 2.6.25, NetBSD >= 8
    final AF_CAN: int
    final PF_CAN: int
    final SOL_CAN_BASE: int
    final SOL_CAN_RAW: int
    final CAN_EFF_FLAG: int
    final CAN_EFF_MASK: int
    final CAN_ERR_FLAG: int
    final CAN_ERR_MASK: int
    final CAN_RAW: int
    final CAN_RAW_FILTER: int
    final CAN_RAW_LOOPBACK: int
    final CAN_RAW_RECV_OWN_MSGS: int
    final CAN_RTR_FLAG: int
    final CAN_SFF_MASK: int
    if sys.version_info < (3, 11):
        final CAN_RAW_ERR_FILTER: int

if sys.platform == "linux":
    # Availability: Linux >= 2.6.25
    final CAN_BCM: int
    final CAN_BCM_TX_SETUP: int
    final CAN_BCM_TX_DELETE: int
    final CAN_BCM_TX_READ: int
    final CAN_BCM_TX_SEND: int
    final CAN_BCM_RX_SETUP: int
    final CAN_BCM_RX_DELETE: int
    final CAN_BCM_RX_READ: int
    final CAN_BCM_TX_STATUS: int
    final CAN_BCM_TX_EXPIRED: int
    final CAN_BCM_RX_STATUS: int
    final CAN_BCM_RX_TIMEOUT: int
    final CAN_BCM_RX_CHANGED: int
    final CAN_BCM_SETTIMER: int
    final CAN_BCM_STARTTIMER: int
    final CAN_BCM_TX_COUNTEVT: int
    final CAN_BCM_TX_ANNOUNCE: int
    final CAN_BCM_TX_CP_CAN_ID: int
    final CAN_BCM_RX_FILTER_ID: int
    final CAN_BCM_RX_CHECK_DLC: int
    final CAN_BCM_RX_NO_AUTOTIMER: int
    final CAN_BCM_RX_ANNOUNCE_RESUME: int
    final CAN_BCM_TX_RESET_MULTI_IDX: int
    final CAN_BCM_RX_RTR_FRAME: int
    final CAN_BCM_CAN_FD_FRAME: int

if sys.platform == "linux":
    # Availability: Linux >= 3.6
    final CAN_RAW_FD_FRAMES: int
    # Availability: Linux >= 4.1
    final CAN_RAW_JOIN_FILTERS: int
    # Availability: Linux >= 2.6.25
    final CAN_ISOTP: int
    if sys.version_info >= (3, 15):
        final CAN_ISOTP_CHK_PAD_DATA: int
        final CAN_ISOTP_CHK_PAD_LEN: int
        final CAN_ISOTP_DEFAULT_EXT_ADDRESS: int
        final CAN_ISOTP_DEFAULT_FLAGS: int
        final CAN_ISOTP_DEFAULT_FRAME_TXTIME: int
        final CAN_ISOTP_DEFAULT_LL_MTU: int
        final CAN_ISOTP_DEFAULT_LL_TX_DL: int
        final CAN_ISOTP_DEFAULT_LL_TX_FLAGS: int
        final CAN_ISOTP_DEFAULT_PAD_CONTENT: int
        final CAN_ISOTP_DEFAULT_RECV_BS: int
        final CAN_ISOTP_DEFAULT_RECV_STMIN: int
        final CAN_ISOTP_DEFAULT_RECV_WFTMAX: int
        final CAN_ISOTP_EXTEND_ADDR: int
        final CAN_ISOTP_FORCE_RXSTMIN: int
        final CAN_ISOTP_FORCE_TXSTMIN: int
        final CAN_ISOTP_HALF_DUPLEX: int
        final CAN_ISOTP_LL_OPTS: int
        final CAN_ISOTP_LISTEN_MODE: int
        final CAN_ISOTP_OPTS: int
        final CAN_ISOTP_RECV_FC: int
        final CAN_ISOTP_RX_EXT_ADDR: int
        final CAN_ISOTP_RX_PADDING: int
        final CAN_ISOTP_RX_STMIN: int
        final CAN_ISOTP_SF_BROADCAST: int
        final CAN_ISOTP_TX_PADDING: int
        final CAN_ISOTP_TX_STMIN: int
        final CAN_ISOTP_WAIT_TX_DONE: int
        final SOL_CAN_ISOTP: int
    # Availability: Linux >= 5.4
    final CAN_J1939: int

    final J1939_MAX_UNICAST_ADDR: int
    final J1939_IDLE_ADDR: int
    final J1939_NO_ADDR: int
    final J1939_NO_NAME: int
    final J1939_PGN_REQUEST: int
    final J1939_PGN_ADDRESS_CLAIMED: int
    final J1939_PGN_ADDRESS_COMMANDED: int
    final J1939_PGN_PDU1_MAX: int
    final J1939_PGN_MAX: int
    final J1939_NO_PGN: int

    final SO_J1939_FILTER: int
    final SO_J1939_PROMISC: int
    final SO_J1939_SEND_PRIO: int
    final SO_J1939_ERRQUEUE: int

    final SCM_J1939_DEST_ADDR: int
    final SCM_J1939_DEST_NAME: int
    final SCM_J1939_PRIO: int
    final SCM_J1939_ERRQUEUE: int

    final J1939_NLA_PAD: int
    final J1939_NLA_BYTES_ACKED: int
    final J1939_EE_INFO_NONE: int
    final J1939_EE_INFO_TX_ABORT: int
    final J1939_FILTER_MAX: int

if sys.version_info >= (3, 12) and sys.platform != "linux" and sys.platform != "win32" and sys.platform != "darwin":
    # Availability: FreeBSD >= 14.0
    final AF_DIVERT: int
    final PF_DIVERT: int

if sys.platform == "linux":
    # Availability: Linux >= 2.2
    final AF_PACKET: int
    final PF_PACKET: int
    final PACKET_BROADCAST: int
    final PACKET_FASTROUTE: int
    final PACKET_HOST: int
    final PACKET_LOOPBACK: int
    final PACKET_MULTICAST: int
    final PACKET_OTHERHOST: int
    final PACKET_OUTGOING: int

if sys.version_info >= (3, 12) and sys.platform == "linux":
    final ETH_P_ALL: int

if sys.platform == "linux":
    # Availability: Linux >= 2.6.30
    final AF_RDS: int
    final PF_RDS: int
    final SOL_RDS: int
    # These are present in include/linux/rds.h but don't always show up
    # here.
    final RDS_CANCEL_SENT_TO: int
    final RDS_CMSG_RDMA_ARGS: int
    final RDS_CMSG_RDMA_DEST: int
    final RDS_CMSG_RDMA_MAP: int
    final RDS_CMSG_RDMA_STATUS: int
    final RDS_CONG_MONITOR: int
    final RDS_FREE_MR: int
    final RDS_GET_MR: int
    final RDS_GET_MR_FOR_DEST: int
    final RDS_RDMA_DONTWAIT: int
    final RDS_RDMA_FENCE: int
    final RDS_RDMA_INVALIDATE: int
    final RDS_RDMA_NOTIFY_ME: int
    final RDS_RDMA_READWRITE: int
    final RDS_RDMA_SILENT: int
    final RDS_RDMA_USE_ONCE: int
    final RDS_RECVERR: int

    # This is supported by CPython but doesn't seem to be a real thing.
    # The closest existing constant in rds.h is RDS_CMSG_CONG_UPDATE
    # RDS_CMSG_RDMA_UPDATE: Final[int]

if sys.platform == "win32":
    final SIO_RCVALL: int
    final SIO_KEEPALIVE_VALS: int
    final SIO_LOOPBACK_FAST_PATH: int
    final RCVALL_MAX: int
    final RCVALL_OFF: int
    final RCVALL_ON: int
    final RCVALL_SOCKETLEVELONLY: int

if sys.platform == "linux":
    final AF_TIPC: int
    final SOL_TIPC: int
    final TIPC_ADDR_ID: int
    final TIPC_ADDR_NAME: int
    final TIPC_ADDR_NAMESEQ: int
    final TIPC_CFG_SRV: int
    final TIPC_CLUSTER_SCOPE: int
    final TIPC_CONN_TIMEOUT: int
    final TIPC_CRITICAL_IMPORTANCE: int
    final TIPC_DEST_DROPPABLE: int
    final TIPC_HIGH_IMPORTANCE: int
    final TIPC_IMPORTANCE: int
    final TIPC_LOW_IMPORTANCE: int
    final TIPC_MEDIUM_IMPORTANCE: int
    final TIPC_NODE_SCOPE: int
    final TIPC_PUBLISHED: int
    final TIPC_SRC_DROPPABLE: int
    final TIPC_SUBSCR_TIMEOUT: int
    final TIPC_SUB_CANCEL: int
    final TIPC_SUB_PORTS: int
    final TIPC_SUB_SERVICE: int
    final TIPC_TOP_SRV: int
    final TIPC_WAIT_FOREVER: int
    final TIPC_WITHDRAWN: int
    final TIPC_ZONE_SCOPE: int

if sys.platform == "linux":
    # Availability: Linux >= 2.6.38
    final AF_ALG: int
    final SOL_ALG: int
    final ALG_OP_DECRYPT: int
    final ALG_OP_ENCRYPT: int
    final ALG_OP_SIGN: int
    final ALG_OP_VERIFY: int
    final ALG_SET_AEAD_ASSOCLEN: int
    final ALG_SET_AEAD_AUTHSIZE: int
    final ALG_SET_IV: int
    final ALG_SET_KEY: int
    final ALG_SET_OP: int
    final ALG_SET_PUBKEY: int

if sys.platform == "linux":
    # Availability: Linux >= 4.8 (or maybe 3.9, CPython docs are confusing)
    final AF_VSOCK: int
    IOCTL_VM_SOCKETS_GET_LOCAL_CID: Final = 0x7B9
    VMADDR_CID_ANY: Final = 0xFFFFFFFF
    VMADDR_CID_HOST: Final = 2
    VMADDR_PORT_ANY: Final = 0xFFFFFFFF
    SO_VM_SOCKETS_BUFFER_MAX_SIZE: Final = 2
    SO_VM_SOCKETS_BUFFER_SIZE: Final = 0
    SO_VM_SOCKETS_BUFFER_MIN_SIZE: Final = 1
    VM_SOCKETS_INVALID_VERSION: Final = 0xFFFFFFFF  # undocumented

# Documented as only available on BSD, macOS, but empirically sometimes
# available on Windows
if sys.platform != "linux":
    final AF_LINK: int

has_ipv6: bool

if sys.platform != "darwin":
    BDADDR_ANY: Final = "00:00:00:00:00:00"
    BDADDR_LOCAL: Final = "00:00:00:FF:FF:FF"

if sys.platform != "win32" and sys.platform != "darwin" and sys.platform != "linux":
    final HCI_FILTER: int  # not in NetBSD or DragonFlyBSD
    final HCI_TIME_STAMP: int  # not in FreeBSD, NetBSD, or DragonFlyBSD
    final HCI_DATA_DIR: int  # not in FreeBSD, NetBSD, or DragonFlyBSD

if sys.platform == "linux":
    final AF_QIPCRTR: int  # Availability: Linux >= 4.7

if sys.version_info >= (3, 11) and sys.platform != "linux" and sys.platform != "win32" and sys.platform != "darwin":
    # FreeBSD
    final SCM_CREDS2: int
    final LOCAL_CREDS: int
    final LOCAL_CREDS_PERSISTENT: int

if sys.version_info >= (3, 11) and sys.platform == "linux":
    final SO_INCOMING_CPU: int  # Availability: Linux >= 3.9

if sys.version_info >= (3, 12) and sys.platform == "win32":
    # Availability: Windows
    final AF_HYPERV: int
    final HV_PROTOCOL_RAW: int
    final HVSOCKET_CONNECT_TIMEOUT: int
    final HVSOCKET_CONNECT_TIMEOUT_MAX: int
    final HVSOCKET_CONNECTED_SUSPEND: int
    final HVSOCKET_ADDRESS_FLAG_PASSTHRU: int
    HV_GUID_ZERO: Final = "00000000-0000-0000-0000-000000000000"
    HV_GUID_WILDCARD: Final = "00000000-0000-0000-0000-000000000000"
    HV_GUID_BROADCAST: Final = "FFFFFFFF-FFFF-FFFF-FFFF-FFFFFFFFFFFF"
    HV_GUID_CHILDREN: Final = "90DB8B89-0D35-4F79-8CE9-49EA0AC8B7CD"
    HV_GUID_LOOPBACK: Final = "E0E16197-DD56-4A10-9195-5EE7A155A838"
    HV_GUID_PARENT: Final = "A42E7CDA-D03F-480C-9CC2-A4DE20ABB878"

if sys.version_info >= (3, 12):
    if sys.platform != "win32":
        # Availability: Linux, FreeBSD, macOS
        final ETHERTYPE_ARP: int
        final ETHERTYPE_IP: int
        final ETHERTYPE_IPV6: int
        final ETHERTYPE_VLAN: int

# --------------------
# Semi-documented constants
# These are alluded to under the "Socket families" section in the docs
# https://docs.python.org/3/library/socket.html#socket-families
# --------------------

if sys.platform == "linux":
    # Netlink is defined by Linux
    final AF_NETLINK: int
    final NETLINK_CRYPTO: int
    final NETLINK_DNRTMSG: int
    final NETLINK_FIREWALL: int
    final NETLINK_IP6_FW: int
    final NETLINK_NFLOG: int
    final NETLINK_ROUTE: int
    final NETLINK_USERSOCK: int
    final NETLINK_XFRM: int
    # Technically still supported by CPython
    # NETLINK_ARPD: Final[int]  # linux 2.0 to 2.6.12 (EOL August 2005)
    # NETLINK_ROUTE6: Final[int]  # linux 2.2 to 2.6.12 (EOL August 2005)
    # NETLINK_SKIP: Final[int]  # linux 2.0 to 2.6.12 (EOL August 2005)
    # NETLINK_TAPBASE: Final[int]  # linux 2.2 to 2.6.12 (EOL August 2005)
    # NETLINK_TCPDIAG: Final[int]  # linux 2.6.0 to 2.6.13 (EOL December 2005)
    # NETLINK_W1: Final[int]  # linux 2.6.13 to 2.6.17 (EOL October 2006)

if sys.platform == "darwin":
    final PF_SYSTEM: int
    final SYSPROTO_CONTROL: int

if sys.platform != "darwin":
    final AF_BLUETOOTH: int

if sys.platform != "win32" and sys.platform != "darwin":
    # Linux and some BSD support is explicit in the docs
    # Windows and macOS do not support in practice
    final BTPROTO_HCI: int
    final BTPROTO_L2CAP: int
    final BTPROTO_SCO: int  # not in FreeBSD
if sys.platform != "darwin":
    final BTPROTO_RFCOMM: int

if sys.platform == "linux":
    final UDPLITE_RECV_CSCOV: int
    final UDPLITE_SEND_CSCOV: int

# --------------------
# Documented under socket.shutdown
# --------------------
final SHUT_RD: int
final SHUT_RDWR: int
final SHUT_WR: int

# --------------------
# Undocumented constants
# --------------------

# Undocumented address families
final AF_APPLETALK: int
final AF_DECnet: int
final AF_IPX: int
final AF_SNA: int

if sys.platform != "win32":
    final AF_ROUTE: int

if sys.platform == "darwin":
    final AF_SYSTEM: int

if sys.platform != "darwin":
    final AF_IRDA: int

if sys.platform != "win32" and sys.platform != "darwin":
    final AF_ASH: int
    final AF_ATMPVC: int
    final AF_ATMSVC: int
    final AF_AX25: int
    final AF_BRIDGE: int
    final AF_ECONET: int
    final AF_KEY: int
    final AF_LLC: int
    final AF_NETBEUI: int
    final AF_NETROM: int
    final AF_PPPOX: int
    final AF_ROSE: int
    final AF_SECURITY: int
    final AF_WANPIPE: int
    final AF_X25: int

# Miscellaneous undocumented

if sys.platform != "win32" and sys.platform != "linux":
    final LOCAL_PEERCRED: int

if sys.platform != "win32" and sys.platform != "darwin":
    # Defined in linux socket.h, but this isn't always present for
    # some reason.
    final IPX_TYPE: int

# ===== Classes =====

@disjoint_base
class socket:
    """socket(family=AF_INET, type=SOCK_STREAM, proto=0) -> socket object
    socket(family=-1, type=-1, proto=-1, fileno=None) -> socket object

    Open a socket of the given type.  The family argument specifies the
    address family; it defaults to AF_INET.  The type argument specifies
    whether this is a stream (SOCK_STREAM, this is the default)
    or datagram (SOCK_DGRAM) socket.  The protocol argument defaults to 0,
    specifying the default protocol.  Keyword arguments are accepted.
    The socket is created as non-inheritable.

    When a fileno is passed in, family, type and proto are auto-detected,
    unless they are explicitly set.

    A socket object represents one endpoint of a network connection.

    Methods of socket objects (keyword arguments not allowed):

    _accept() -- accept connection, returning new socket fd and client address
    bind(addr) -- bind the socket to a local address
    close() -- close the socket
    connect(addr) -- connect the socket to a remote address
    connect_ex(addr) -- connect, return an error code instead of an exception
    dup() -- return a new socket fd duplicated from fileno()
    fileno() -- return underlying file descriptor
    getpeername() -- return remote address [*]
    getsockname() -- return local address
    getsockopt(level, optname[, buflen]) -- get socket options
    gettimeout() -- return timeout or None
    listen([n]) -- start listening for incoming connections
    recv(buflen[, flags]) -- receive data
    recv_into(buffer[, nbytes[, flags]]) -- receive data (into a buffer)
    recvfrom(buflen[, flags]) -- receive data and sender's address
    recvfrom_into(buffer[, nbytes, [, flags]])
      -- receive data and sender's address (into a buffer)
    sendall(data[, flags]) -- send all data
    send(data[, flags]) -- send data, may not send all of it
    sendto(data[, flags], addr) -- send data to a given address
    setblocking(bool) -- set or clear the blocking I/O flag
    getblocking() -- return True if socket is blocking, False if non-blocking
    setsockopt(level, optname, value[, optlen]) -- set socket options
    settimeout(None | float) -- set or clear the timeout
    shutdown(how) -- shut down traffic in one or both directions

     [*] not available on all platforms!
    """

    let family: int

    let type: int

    let proto: int

    # F811: "Redefinition of unused `timeout`"
    let timeout: float | None

    if sys.platform == "win32":
        def __init__(
            self, family: int = ..., type: int = ..., proto: int = ..., fileno: SupportsIndex | bytes | None = None
        ) -> None
    else:
        def __init__(self, family: int = ..., type: int = ..., proto: int = ..., fileno: SupportsIndex | None = None) -> None

    def bind(self, address: _Address, /):
        """bind(address)

        Bind the socket to a local address.  For IP sockets, the address is a
        pair (host, port); the host must refer to the local host. For raw packet
        sockets the address is a tuple (ifname, proto [,pkttype [,hatype [,addr]]])
        """

    def close(self):
        """close()

        Close the socket.  It cannot be used after this call.
        """

    def connect(self, address: _Address, /):
        """connect(address)

        Connect the socket to a remote address.  For IP sockets, the address
        is a pair (host, port).
        """

    def connect_ex(self, address: _Address, /) -> int:
        """connect_ex(address) -> errno

        This is like connect(address), but returns an error code (the errno value)
        instead of raising an exception when an error occurs.
        """

    def detach(self) -> int:
        """detach()

        Close the socket object without closing the underlying file descriptor.
        The object cannot be used after this call, but the file descriptor
        can be reused for other purposes.  The file descriptor is returned.
        """

    def fileno(self) -> int:
        """fileno() -> integer

        Return the integer file descriptor of the socket.
        """

    def getpeername(self) -> _RetAddress:
        """getpeername() -> address info

        Return the address of the remote endpoint.  For IP sockets, the address
        info is a pair (hostaddr, port).
        """

    def getsockname(self) -> _RetAddress:
        """getsockname() -> address info

        Return the address of the local endpoint. The format depends on the
        address family. For IPv4 sockets, the address info is a pair
        (hostaddr, port). For IPv6 sockets, the address info is a 4-tuple
        (hostaddr, port, flowinfo, scope_id).
        """

    def getsockopt(self, level: int, optname: int, /) -> int:
        """getsockopt(level, option[, buffersize]) -> value

        Get a socket option.  See the Unix manual for level and option.
        If a nonzero buffersize argument is given, the return value is a
        string of that length; otherwise it is an integer.
        """
    def getsockopt(self, level: int, optname: int, buflen: int, /) -> bytes

    def getblocking(self) -> bool:
        """getblocking()

        Returns True if socket is in blocking mode, or False if it
        is in non-blocking mode.
        """

    def gettimeout(self) -> float | None:
        """gettimeout() -> timeout

        Returns the timeout in seconds (float) associated with socket
        operations. A timeout of None indicates that timeouts on socket
        operations are disabled.
        """

    if sys.platform == "win32":
        def ioctl(self, control: int, option: int | (int, int, int) | bool, /):
            """ioctl(cmd, option) -> long

            Control the socket with WSAIoctl syscall. Currently supported 'cmd' values are
            SIO_RCVALL:  'option' must be one of the socket.RCVALL_* constants.
            SIO_KEEPALIVE_VALS:  'option' is a tuple of (onoff, timeout, interval).
            SIO_LOOPBACK_FAST_PATH: 'option' is a boolean value, and is disabled by default
            """

    def listen(self, backlog: int = ..., /):
        """listen([backlog])

        Enable a server to accept connections.  If backlog is specified, it must be
        at least 0 (if it is lower, it is set to 0); it specifies the number of
        unaccepted connections that the system will allow before refusing new
        connections. If not specified, a default reasonable value is chosen.
        """

    def recv(self, bufsize: int, flags: int = 0, /) -> bytes:
        """recv(buffersize[, flags]) -> data

        Receive up to buffersize bytes from the socket.  For the optional flags
        argument, see the Unix manual.  When no data is available, block until
        at least one byte is available or until the remote end is closed.  When
        the remote end is closed and all data is read, return the empty string.
        """

    def recvfrom(self, bufsize: int, flags: int = 0, /) -> (bytes, _RetAddress):
        """recvfrom(buffersize[, flags]) -> (data, address info)

        Like recv(buffersize, flags) but also return the sender's address info.
        """

    if sys.platform != "win32":
        def recvmsg(self, bufsize: int, ancbufsize: int = 0, flags: int = 0, /) -> (bytes, list[CMSG], int, dynamic):
            """recvmsg(bufsize[, ancbufsize[, flags]]) -> (data, ancdata, msg_flags, address)

            Receive normal data (up to bufsize bytes) and ancillary data from the
            socket.  The ancbufsize argument sets the size in bytes of the
            internal buffer used to receive the ancillary data; it defaults to 0,
            meaning that no ancillary data will be received.  Appropriate buffer
            sizes for ancillary data can be calculated using CMSG_SPACE() or
            CMSG_LEN(), and items which do not fit into the buffer might be
            truncated or discarded.  The flags argument defaults to 0 and has the
            same meaning as for recv().

            The return value is a 4-tuple: (data, ancdata, msg_flags, address).
            The data item is a bytes object holding the non-ancillary data
            received.  The ancdata item is a list of zero or more tuples
            (cmsg_level, cmsg_type, cmsg_data) representing the ancillary data
            (control messages) received: cmsg_level and cmsg_type are integers
            specifying the protocol level and protocol-specific type respectively,
            and cmsg_data is a bytes object holding the associated data.  The
            msg_flags item is the bitwise OR of various flags indicating
            conditions on the received message; see your system documentation for
            details.  If the receiving socket is unconnected, address is the
            address of the sending socket, if available; otherwise, its value is
            unspecified.

            If recvmsg() raises an exception after the system call returns, it
            will first attempt to close any file descriptors received via the
            SCM_RIGHTS mechanism.
            """

        def recvmsg_into(
            self, buffers: Iterable[WriteableBuffer], ancbufsize: int = 0, flags: int = 0, /
        ) -> (int, list[CMSG], int, dynamic):
            """recvmsg_into(buffers[, ancbufsize[, flags]]) -> (nbytes, ancdata, msg_flags, address)

            Receive normal data and ancillary data from the socket, scattering the
            non-ancillary data into a series of buffers.  The buffers argument
            must be an iterable of objects that export writable buffers
            (e.g. bytearray objects); these will be filled with successive chunks
            of the non-ancillary data until it has all been written or there are
            no more buffers.  The ancbufsize argument sets the size in bytes of
            the internal buffer used to receive the ancillary data; it defaults to
            0, meaning that no ancillary data will be received.  Appropriate
            buffer sizes for ancillary data can be calculated using CMSG_SPACE()
            or CMSG_LEN(), and items which do not fit into the buffer might be
            truncated or discarded.  The flags argument defaults to 0 and has the
            same meaning as for recv().

            The return value is a 4-tuple: (nbytes, ancdata, msg_flags, address).
            The nbytes item is the total number of bytes of non-ancillary data
            written into the buffers.  The ancdata item is a list of zero or more
            tuples (cmsg_level, cmsg_type, cmsg_data) representing the ancillary
            data (control messages) received: cmsg_level and cmsg_type are
            integers specifying the protocol level and protocol-specific type
            respectively, and cmsg_data is a bytes object holding the associated
            data.  The msg_flags item is the bitwise OR of various flags
            indicating conditions on the received message; see your system
            documentation for details.  If the receiving socket is unconnected,
            address is the address of the sending socket, if available; otherwise,
            its value is unspecified.

            If recvmsg_into() raises an exception after the system call returns,
            it will first attempt to close any file descriptors received via the
            SCM_RIGHTS mechanism.
            """

    def recvfrom_into(self, buffer: WriteableBuffer, nbytes: int = 0, flags: int = 0) -> (int, _RetAddress):
        """recvfrom_into(buffer[, nbytes[, flags]]) -> (nbytes, address info)

        Like recv_into(buffer[, nbytes[, flags]]) but also return the sender's address info.
        """

    def recv_into(self, buffer: WriteableBuffer, nbytes: int = 0, flags: int = 0) -> int:
        """recv_into(buffer, [nbytes[, flags]]) -> nbytes_read

        A version of recv() that stores its data into a buffer rather than creating
        a new string.  Receive up to buffersize bytes from the socket.  If buffersize
        is not specified (or 0), receive up to the size available in the given buffer.

        See recv() for documentation about the flags.
        """

    def send(self, data: ReadableBuffer, flags: int = 0, /) -> int:
        """Send a data string to the socket.

        For the optional flags argument, see the Unix manual.
        Return the number of bytes sent; this may be less than len(data) if
        the network is busy.
        """

    def sendall(self, data: ReadableBuffer, flags: int = 0, /):
        """Send a data string to the socket.

        For the optional flags argument, see the Unix manual.
        This calls send() repeatedly until all data is sent.
        If an error occurs, it's impossible to tell how much data has been
        sent.
        """

    def sendto(self, data: ReadableBuffer, address: _Address, /) -> int:
        """sendto(data[, flags], address) -> count

        Like send(data, flags) but allows specifying the destination address.
        For IP sockets, the address is a pair (hostaddr, port).
        """
    def sendto(self, data: ReadableBuffer, flags: int, address: _Address, /) -> int

    if sys.platform != "win32":
        def sendmsg(
            self,
            buffers: Iterable[ReadableBuffer],
            ancdata: Iterable[CMSGArg] = ...,
            flags: int = 0,
            address: _Address | None = None,
            /,
        ) -> int:
            """Send normal and ancillary data to the socket.

            It gathering the non-ancillary data from a series of buffers
            and concatenating it into a single message.
            The buffers argument specifies the non-ancillary
            data as an iterable of bytes-like objects (e.g. bytes objects).
            The ancdata argument specifies the ancillary data (control messages)
            as an iterable of zero or more tuples (cmsg_level, cmsg_type,
            cmsg_data), where cmsg_level and cmsg_type are integers specifying
            the protocol level and protocol-specific type respectively, and
            cmsg_data is a bytes-like object holding the associated data.  The
            flags argument defaults to 0 and has the same meaning as for send().
            If address is supplied and not None, it sets a destination address
            for the message.  The return value is the number of bytes of
            non-ancillary data sent.
            """

    if sys.platform == "linux":
        def sendmsg_afalg(
            self, msg: Iterable[ReadableBuffer] = ..., *, op: int, iv: dynamic = ..., assoclen: int = ..., flags: int = 0
        ) -> int:
            """sendmsg_afalg([msg], *, op[, iv[, assoclen[, flags=MSG_MORE]]])

            Set operation mode, IV and length of associated data for an AF_ALG
            operation socket.
            """

    def setblocking(self, flag: bool, /):
        """setblocking(flag)

        Set the socket to blocking (flag is true) or non-blocking (false).
        setblocking(True) is equivalent to settimeout(None);
        setblocking(False) is equivalent to settimeout(0.0).
        """

    def settimeout(self, value: int | float | None, /):
        """settimeout(timeout)

        Set a timeout on socket operations.  'timeout' can be a float,
        giving in seconds, or None.  Setting a timeout of None disables
        the timeout feature and is equivalent to setblocking(1).
        Setting a timeout of zero is the same as setblocking(0).
        """

    def setsockopt(self, level: int, optname: int, value: int | ReadableBuffer, /) -> None:
        """setsockopt(level, option, value: int)
        setsockopt(level, option, value: buffer)
        setsockopt(level, option, None, optlen: int)

        Set a socket option.  See the Unix manual for level and option.
        The value argument can either be an integer, a string buffer, or
        None, optlen.
        """
    def setsockopt(self, level: int, optname: int, value: None, optlen: int, /) -> None

    if sys.platform == "win32":
        def share(self, process_id: int, /) -> bytes:
            """share(process_id) -> bytes

            Share the socket with another process.  The target process id
            must be provided and the resulting bytes object passed to the target
            process.  There the shared socket can be instantiated by calling
            socket.fromshare().
            """

    def shutdown(self, how: int, /):
        """shutdown(flag)

        Shut down the reading side of the socket (flag == SHUT_RD), the writing side
        of the socket (flag == SHUT_WR), or both ends (flag == SHUT_RDWR).
        """

SocketType = socket

# ===== Functions =====

def close(fd: SupportsIndex, /):
    """close(integer) -> None

    Close an integer socket file descriptor.  This is like os.close(), but for
    sockets; on some platforms os.close() won't work for socket file descriptors.
    """

def dup(fd: SupportsIndex, /) -> int:
    """dup(integer) -> integer

    Duplicate an integer socket file descriptor.  This is like os.dup(), but for
    sockets; on some platforms os.dup() won't work for socket file descriptors.
    """

# the 5th tuple item is an address
def getaddrinfo(
    host: bytes | str | None, port: bytes | str | int | None, family: int = ..., type: int = 0, proto: int = 0, flags: int = 0
) -> list[(int, int, int, str, (str, int) | (str, int, int, int) | (int, bytes))]:
    """getaddrinfo(host, port [, family, type, proto, flags])
        -> list of (family, type, proto, canonname, sockaddr)

    Resolve host and port into addrinfo struct.
    """

def gethostbyname(hostname: str, /) -> str:
    """gethostbyname(host) -> address

    Return the IP address (a string of the form '255.255.255.255') for a host.
    """

def gethostbyname_ex(hostname: str, /) -> (str, list[str], list[str]):
    """gethostbyname_ex(host) -> (name, aliaslist, addresslist)

    Return the true host name, a list of aliases, and a list of IP addresses,
    for a host.  The host argument is a string giving a host name or IP number.
    """

def gethostname() -> str:
    """gethostname() -> string

    Return the current host name.
    """

def gethostbyaddr(ip_address: str, /) -> (str, list[str], list[str]):
    """gethostbyaddr(host) -> (name, aliaslist, addresslist)

    Return the true host name, a list of aliases, and a list of IP addresses,
    for a host.  The host argument is a string giving a host name or IP number.
    """

def getnameinfo(sockaddr: (str, int) | (str, int, int, int) | (int, bytes), flags: int, /) -> (str, str):
    """getnameinfo(sockaddr, flags) --> (host, port)

    Get host and port for a sockaddr.
    """

def getprotobyname(protocolname: str, /) -> int:
    """getprotobyname(name) -> integer

    Return the protocol number for the named protocol.  (Rarely used.)
    """

def getservbyname(servicename: str, protocolname: str = ..., /) -> int:
    """getservbyname(servicename[, protocolname]) -> integer

    Return a port number from a service name and protocol name.
    The optional protocol name, if given, should be 'tcp' or 'udp',
    otherwise any protocol will match.
    """

def getservbyport(port: int, protocolname: str = ..., /) -> str:
    """getservbyport(port[, protocolname]) -> string

    Return the service name from a port number and protocol name.
    The optional protocol name, if given, should be 'tcp' or 'udp',
    otherwise any protocol will match.
    """

def ntohl(x: int, /) -> int:  # param & ret val are 32-bit ints
    """Convert a 32-bit unsigned integer from network to host byte order."""

def ntohs(x: int, /) -> int:  # param & ret val are 16-bit ints
    """Convert a 16-bit unsigned integer from network to host byte order."""

def htonl(x: int, /) -> int:  # param & ret val are 32-bit ints
    """Convert a 32-bit unsigned integer from host to network byte order."""

def htons(x: int, /) -> int:  # param & ret val are 16-bit ints
    """Convert a 16-bit unsigned integer from host to network byte order."""

def inet_aton(ip_addr: str, /) -> bytes:  # ret val 4 bytes in length
    """Convert an IP address in string format (123.45.67.89) to the 32-bit packed binary format used in low-level network functions."""

def inet_ntoa(packed_ip: ReadableBuffer, /) -> str:
    """Convert an IP address from 32-bit packed binary format to string format."""

def inet_pton(address_family: int, ip_string: str, /) -> bytes:
    """inet_pton(af, ip) -> packed IP address string

    Convert an IP address from string format to a packed string suitable
    for use with low-level network functions.
    """

def inet_ntop(address_family: int, packed_ip: ReadableBuffer, /) -> str:
    """inet_ntop(af, packed_ip) -> string formatted IP address

    Convert a packed IP address of the given family to string format.
    """

def getdefaulttimeout() -> float | None:
    """getdefaulttimeout() -> timeout

    Returns the default timeout in seconds (float) for new socket objects.
    A value of None indicates that new socket objects have no timeout.
    When the socket module is first imported, the default is None.
    """

# F811: "Redefinition of unused `timeout`"
def setdefaulttimeout(timeout: int | float | None, /):
    """setdefaulttimeout(timeout)

    Set the default timeout in seconds (real number) for new socket objects.
    A value of None indicates that new socket objects have no timeout.
    When the socket module is first imported, the default is None.
    """

if sys.platform != "win32":
    def sethostname(name: str, /):
        """sethostname(name)

        Sets the hostname to name.
        """

    def CMSG_LEN(length: int, /) -> int:
        """CMSG_LEN(length) -> control message length

        Return the total length, without trailing padding, of an ancillary
        data item with associated data of the given length.  This value can
        often be used as the buffer size for recvmsg() to receive a single
        item of ancillary data, but RFC 3542 requires portable applications to
        use CMSG_SPACE() and thus include space for padding, even when the
        item will be the last in the buffer.  Raises OverflowError if length
        is outside the permissible range of values.
        """

    def CMSG_SPACE(length: int, /) -> int:
        """CMSG_SPACE(length) -> buffer size

        Return the buffer size needed for recvmsg() to receive an ancillary
        data item with associated data of the given length, along with any
        trailing padding.  The buffer space needed to receive multiple items
        is the sum of the CMSG_SPACE() values for their associated data
        lengths.  Raises OverflowError if length is outside the permissible
        range of values.
        """

    def socketpair(family: int = ..., type: int = ..., proto: int = 0, /) -> (socket, socket):
        """socketpair([family[, type [, proto]]]) -> (socket object, socket object)

        Create a pair of socket objects from the sockets returned by the platform
        socketpair() function.
        The arguments are the same as for socket() except the default family is
        AF_UNIX if defined on the platform; otherwise, the default is AF_INET.
        """

def if_nameindex() -> list[(int, str)]:
    """if_nameindex()

    Returns a list of network interface information (index, name) tuples.
    """

def if_nametoindex(oname: str, /) -> int:
    """Returns the interface index corresponding to the interface name if_name."""

if sys.version_info >= (3, 14):
    def if_indextoname(if_index: int, /) -> str:
        """Returns the interface name corresponding to the interface index if_index."""

else:
    def if_indextoname(index: int, /) -> str:
        """if_indextoname(if_index)

        Returns the interface name corresponding to the interface index if_index.
        """

CAPI: CapsuleType

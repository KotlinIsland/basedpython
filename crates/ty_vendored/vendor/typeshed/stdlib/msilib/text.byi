import sys
from typing import Final

if sys.platform == "win32":
    final ActionText: list[(str, str, str | None)]
    final UIText: list[(str, str | None)]
    dirname: str
    final tables: list[str]

"""distutils.command.check

Implements the Distutils 'check' command.
"""

from _typeshed import Incomplete
from typing import ClassVar, Final, Literal, TypeAlias

from ..cmd import Command

_Reporter: TypeAlias = dynamic  # really docutils.utils.Reporter

# Only defined if docutils is installed.
# Depends on a third-party stub. Since distutils is deprecated anyway,
# it's easier to just suppress the "any subclassing" error.
class SilentReporter(_Reporter):
    messages: Incomplete
    init(
        self,
        source,
        report_level,
        halt_level,
        stream: Incomplete | None = ...,
        debug: bool | 0 | 1 = 0,
        encoding: str = ...,
        error_handler: str = ...,
    )
    def system_message(self, level, message, *children, **kwargs)

final HAS_DOCUTILS: bool

class check(Command):
    """This command checks the meta-data of the package."""

    description: str
    user_options: ClassVar[list[(str, str, str)]]
    boolean_options: ClassVar[list[str]]
    restructuredtext: int
    metadata: int
    strict: int
    override def initialize_options(self):
        """Sets default values for options."""

    override def finalize_options(self)
    override def warn(self, msg):
        """Counts the number of warnings that occurs."""

    override def run(self):
        """Runs the command."""

    def check_metadata(self):
        """Ensures that all required elements of meta-data are supplied.

        Required fields:
            name, version, URL

        Recommended fields:
            (author and author_email) or (maintainer and maintainer_email)

        Warns if any are missing.
        """

    def check_restructuredtext(self):
        """Checks if the long string fields are reST-compliant."""

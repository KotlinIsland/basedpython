"""Implements (a subset of) Sun XDR -- eXternal Data Representation.

See: RFC 1014

"""

from collections.abc import Callable, Sequence
from typing import TypeVar

__all__ = ["Error", "Packer", "Unpacker", "ConversionError"]


class Error(Exception):
    """Exception class for this module. Use:

    except xdrlib.Error as var:
        # var has the Error instance for the exception

    Public ivars:
        msg -- contains the message

    """

    msg: str
    init(self, msg: str)

class ConversionError(Error)

class Packer:
    """Pack various data representations into a buffer."""

    def reset(self)
    def get_buffer(self) -> bytes
    def get_buf(self) -> bytes
    def pack_uint(self, x: int)
    def pack_int(self, x: int)
    def pack_enum(self, x: int)
    def pack_bool(self, x: bool)
    def pack_uhyper(self, x: int)
    def pack_hyper(self, x: int)
    def pack_float(self, x: float)
    def pack_double(self, x: float)
    def pack_fstring(self, n: int, s: bytes)
    def pack_fopaque(self, n: int, s: bytes)
    def pack_string(self, s: bytes)
    def pack_opaque(self, s: bytes)
    def pack_bytes(self, s: bytes)
    def pack_list[Element](self, list: Sequence[Element], pack_item: (Element) -> object)
    def pack_farray[Element](self, n: int, list: Sequence[Element], pack_item: (Element) -> object)
    def pack_array[Element](self, list: Sequence[Element], pack_item: (Element) -> object)

class Unpacker:
    """Unpacks various data representations from the given buffer."""

    init(self, data: bytes)
    def reset(self, data: bytes)
    def get_position(self) -> int
    def set_position(self, position: int)
    def get_buffer(self) -> bytes
    def done(self)
    def unpack_uint(self) -> int
    def unpack_int(self) -> int
    def unpack_enum(self) -> int
    def unpack_bool(self) -> bool
    def unpack_uhyper(self) -> int
    def unpack_hyper(self) -> int
    def unpack_float(self) -> float
    def unpack_double(self) -> float
    def unpack_fstring(self, n: int) -> bytes
    def unpack_fopaque(self, n: int) -> bytes
    def unpack_string(self) -> bytes
    def unpack_opaque(self) -> bytes
    def unpack_bytes(self) -> bytes
    def unpack_list[Element](self, unpack_item: () -> Element) -> list[Element]
    def unpack_farray[Element](self, n: int, unpack_item: () -> Element) -> list[Element]
    def unpack_array[Element](self, unpack_item: () -> Element) -> list[Element]

import sys
from _typeshed import StrOrBytesPath
from types import FrameType

from .tasks import Task

def _task_repr_info(task: Task[dynamic]) -> list[str]  # undocumented

if sys.version_info >= (3, 13):
    def _task_repr(task: Task[dynamic]) -> str  # undocumented

elif sys.version_info >= (3, 11):
    def _task_repr(self: Task[dynamic]) -> str  # undocumented

def _task_get_stack(task: Task[dynamic], limit: int?) -> list[FrameType]  # undocumented
def _task_print_stack(task: Task[dynamic], limit: int?, file: StrOrBytesPath)  # undocumented

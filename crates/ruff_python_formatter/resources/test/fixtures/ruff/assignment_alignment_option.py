# the default aligns these; the second output shows the opt-out
short = 1
much_longer = 2
counter += 3
annotated: int = 4
first, second = 5, 6
chained = also = 7


def f():
    """Code examples in docstrings follow the same setting.

    ```python
    x = 1
    yyy = 2
    ```
    """

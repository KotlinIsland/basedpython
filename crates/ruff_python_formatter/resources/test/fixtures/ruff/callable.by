f: (int, str) -> bool
g: (a: int, b: str) -> bool
h: (int, /, name: str) -> bool
i: (int, *, name: str) -> bool
j: (*: int) -> int
k: (**: str) -> int
l: (int, /, name: str, *args: bool, **kwargs: int) -> None
m: (...) -> int
n: (...) -> (int) -> str
o: (*Ts) -> int
p: (int, *Ts) -> int
q: (*args: *Ts) -> int
r: (Unpack[Ts]) -> int
s: (int, Unpack[tuple[str, bytes]]) -> int


def takes(cb: (int) -> str) -> str:
    return cb(0)


o: int.() -> str
p: str.(int) -> bytes
q: int.(str, /, name: bytes) -> None
q2: int.(str, *, flag: bool) -> None
r: list[int.() -> str]
s: int.() -> str.() -> bytes
t: int.(...) -> str


def takes_receiver(cb: int.() -> str) -> str:
    return cb(0)


# the verbatim range carries the comments written between the parameters
type Commented = (
    int,  # end-of-line on a parameter
    # own-line between parameters
    str,
) -> bool

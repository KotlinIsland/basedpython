---
source: crates/mdtest/src/lib.rs
expression: snapshot
---

---
mdtest name: basedpython_used_underscore_name.md - used underscore names - the report points at the declaration it resolved to
mdtest path: crates/ty_python_semantic/resources/mdtest/basedpython_used_underscore_name.md
---

# Source files

## shared.by

```
1 | def _parse(text: str) -> int:
2 |     return int(text)
```

## main.by

```
1 | from shared import _parse  # error: [used-underscore-name]
2 | 
3 | _parse("1")  # error: [used-underscore-name]
```

# Diagnostics

```
warning[used-underscore-name]: `_parse` is used, but its leading underscore marks it unused
 --> src/main.by:1:20
  |
1 | from shared import _parse  # error: [used-underscore-name]
  |                    ^^^^^^
  |
 ::: src/shared.by:1:5
  |
1 | def _parse(text: str) -> int:
  |     ------ `_parse` is declared here
info: drop the underscore, or declare it `private` if it is meant to be private

```

```
warning[used-underscore-name]: `_parse` is used, but its leading underscore marks it unused
 --> src/main.by:3:1
  |
3 | _parse("1")  # error: [used-underscore-name]
  | ^^^^^^
  |
 ::: src/shared.by:1:5
  |
1 | def _parse(text: str) -> int:
  |     ------ `_parse` is declared here
info: drop the underscore, or declare it `private` if it is meant to be private

```
